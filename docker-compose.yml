version: "3.9"

services:
  spotify-predictor:
    build: .
    container_name: spotify-hit-predictor
    ports:
      - "8000:8000"
    volumes:
      - ./model:/app/model
    restart: unless-stopped